function R = move_robot(params)
    x = params.x;
    y = params.y;
    z = params.z;
    roll = params.roll;
    pitch = params.pitch;
    yaw = params.yaw;
    wrench = params.wrench;
    lx = params.lx;
    ly = params.ly;
    lz = params.lz;
    lroll = params.lroll;
    lpitch = params.lpitch;
    lyaw = params.lyaw;
    lrobot = make_robot();
    R = [];
end